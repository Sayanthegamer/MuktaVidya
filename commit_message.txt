🧹 Fix hardcoded values and improve lint compliance

🎯 What: Replaced hardcoded hex colors in `DiagramRenderer.tsx` with CSS variables from the global theme. Additionally, fixed typing errors and unnecessary lint rule disable directives in `SolutionPanel.tsx` and `UploadZone.tsx`.
💡 Why: This improves maintainability by ensuring that the ECharts colors respond correctly to the global theme, improving visual consistency and allowing easier updates across the app. Also addressed linting problems to enhance code health.
✅ Verification: Ran `pnpm test` successfully ensuring that all 45 tests, including rendering tests in `DiagramRenderer.test.tsx`, passed. Ran `pnpm lint` and `test_slop_audit.sh` which executed a successful production build and confirmed the code type-checks properly.
✨ Result: `DiagramRenderer` now reliably uses design-system CSS variables without regressions, and the codebase remains free of build and lint errors.
