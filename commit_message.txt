✨ Implement Continuous Chat and Alien Floating Dock

🎯 What
- Transformed the static solution view into a continuous chat interface, allowing follow-up questions.
- Added an "Alien" floating dock at the bottom of the screen with a breathing pure CSS edge glow, text input, and image attachment capability.
- Updated the API endpoint to support passing a full conversation history (messages array).

💡 Why
- Users needed a way to ask follow-up questions to clarify solutions or dive deeper.
- The floating dock keeps the interaction contextual and accessible without replacing the original layout logic.
- The CSS-only glow implementation stays true to the architectural performance limits, avoiding heavy React framer-motion components or expensive reflows.

✅ Verification
- Next.js build passes cleanly.
- TypeScript compiler verified all updated prop interfaces and payload updates.
- Linter passed cleanly without any sweeping auto-fixes or out-of-scope slop.

✨ Result
- The user can now maintain contextual chat streams within the same UI panel, and the platform feels dynamic and alien as requested.
