🧹 Extract nested ternary tooltip string to function

🎯 **What:** Extract tooltip deeply nested ternary logic on FloatingDock into a single standalone string generator `getSendTooltipTitle`. Fixed several React ref access types and unnecessary ESLint disables during check.
💡 **Why:** Significantly improves the readability of the FloatingDock.tsx render code. Prevent complex multiline conditional strings from taking up component template area.
✅ **Verification:** Verified safe via `pnpm lint`, `pnpm test`, and Next build verification logic (`./test_slop_audit.sh`). No new TypeScript errors introduced.
✨ **Result:** A fully decoupled render text generation that does not interrupt the JSX element declaration, with better fallback ref initialization states in `SolutionPanel.tsx` eliminating `any` types.
